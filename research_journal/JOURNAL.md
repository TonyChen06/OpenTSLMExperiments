# Research Journal — ICLR TS-LM Paper · SSM era (built on OpenTSLM)

> **⭐ READ THIS FIRST each session.** This is the durable lab notebook for the CURRENT direction.
> The harness memory index (`~/.claude/.../memory/MEMORY.md`) is auto-loaded and holds the high-level
> decisions; THIS holds the detail + running LOG. Pre-SSM history (AHRI, context-forecasting/CiK,
> neuro-symbolic Chronos) is archived in `research_journal/archive/` — do NOT resurrect it.

## 0. CURRENT STATE (one screen)
- **★ CURRENT FOCUS (2026-06-18): FIRST EXPERIMENT SET DONE — the architectural-justification Table-1 is COMPLETE.** 5 models × 3 tasks (TSQA MCQ, HAR-CoT, Sleep-CoT), identical 50-epoch protocol (patience-5, warmup-10%, raw-best ckpt), two 4×A100 boxes, scored paper-consistently with `tslm_study/codebase_f1.py`. **HEADLINE: SSM (Mamba)+tokens tops EVERY task** — macro-F1 (TSQA/HAR/Sleep): mamba-1.4b **0.998/0.671/0.627** #1 everywhere; mamba-370m 0.997/0.663/0.608 beats ALL attn baselines; SP 0.925/0.650/0.584, llama_bins 0.943/0.624/0.486, Flamingo 0.920/0.570/0.434. Numbers + per-epoch logs in `results_logs/RESULTS.md` (boxA=SP+mamba-1.4b, boxB=llama_bins/mamba-370m/Flamingo). Figures: `assets/tslm_{f1,acc,valloss,cot_examples}.png` + `paper/figs/*.pdf` (generators `paper/figs/fig_{results,valloss,cot_examples}.py`). **Eval lessons (memory [[ref-eval-cot-gotchas]]):** score with codebase_f1 (naive 'Answer:' WRONG for Sleep — stage-merge); CoT val-loss ≠ F1 (SP lowest CoT val-loss but loses F1); Flamingo open_flamingo never stops at EOS → cap CoT max_new_tokens=256 + truncate ramble; SP must not pass position_ids to generate(); torch.compile `_orig_mod` loader fix; Sleep-EDF OOM → `--gradient_checkpointing` (Mamba/Llama only). **NEXT = SECOND experiment set** (the "beat everyone" / regime-advantage phase — long-signal, state-cache, dense per-step; architecture comparison is settled, build the headline contributions on the fully-SSM tokenized base). Memory [[project-table1-architecture-result]].
- **Direction (LOCKED):** a *fully-SSM* (Mamba) time-series LM, Type-A "**beat everyone**" paper. The signal goes IN and OUT as **tokens** in the same stream as text — **no encoder, no fusion module, no head** ("pure token"). Edge = the **regimes** the SSM unlocks (long-signal, state-cache multi-query, dense per-step) + competitive elsewhere. Memory: [[project-direction-context-forecasting]], [[project-iclr-tslm-paper]].
- **Representation = `quant` (DECIDED 2026-06-06) — for OUTPUT-capability + pure-token thesis, NOT input accuracy.** TSQA-370m quant 0.926 ≫ linear 0.860, BUT **lr-fair @1.4b they TIE** (quant 0.914 ≈ linear 0.920) — the edge is a *small-model* effect; soft-prompt catches up at scale. Keep quant because it's the only rep that can *generate* signal (forecasting/gen regimes) + clean no-encoder/no-head story — NOT because it reads better (don't claim that). **lr-fragile @scale:** use lr ≤2e-5 @1.4b full-FT (5e-5 destabilizes the 256 new bin tokens → 0.8055 crash). floats ruled out (OOMs @1.4b). LoRA-backbone + trainable bin-embeddings being ablated as the lr-robust route. Memory: [[project-findings-representation]].
- **Base model:** `state-spaces/mamba-1.4b-hf` = the cheap **iteration workhorse** (weak Pile base, but base-internal comparisons control for it). Escalate to **Zamba2-2.7B** (interim strong) / **Falcon-Mamba-7B** (pure, strong) ONLY at the transformer head-to-head. Memory: [[ref-base-model-options]].
- **What's the contribution backlog / queue:** [[project-idea-backlog]] (6-agent recon, ranked). Tiers: T1 representation (done: quant) + bidir; T2 forecasting-output→CiK / dense-sleep / state-cache-ECG-QA; T3 hierarchical-VQ etc. Competitor numbers to beat: [[ref-target-leaderboard]].
- **Eval rigor:** [[feedback-eval-rigor-test-size]] — use **test-n ≥ 2000** for any decision; **TSQA is the SENSITIVE TESTBED** for input-side experiments (HAR is too noisy). Working prefs: [[feedback-compute-care]], [[feedback-keep-gpus-busy]], [[feedback-prefer-method-papers]], [[feedback-autonomous-operation]].

## 1. Environment & harness (how to actually run)
- **ALWAYS `source tslm_env.sh` first** (repo root). It sets `$PY` = `.venv/bin/python`, `PYTHONPATH=src`, `HF_HOME`, and the **`LD_LIBRARY_PATH` fix** (system CUDA-12.9 cuBLASLt was shadowing torch's → conv/kernels crashed). Without it, CUDA ops abort.
- **Model:** `src/opentslm/model/llm/MambaTSLM.py` — fully-SSM, `TimeSeriesLLM` interface. Signal modes via `encoder=`: `quant` (value-bin tokens, OUR choice), `floats` (digit text), `linear/mlp/conv` (vector encoders); `bidir=True` = feed signal forward+reversed (pure-token bidirectional). **Batched RIGHT-pad train / LEFT-pad gen** (the fused mamba-ssm kernel produces nan grads on left-padded backward — see code comments; right-pad + no-attention-mask avoids it).
- **Harness:** `tslm_study/run_har.py` (HAR-CoT, F1) and `tslm_study/run_tsqa.py` (TSQA, letter-acc — **the sensitive testbed**). Both reuse `build_model` + `collate` + `--save/--ckpt` + per-run YAML. Flags: `--encoder --bidir --lora-r --seed --train-n --epochs --bs --test-n --save --ckpt`.
- **Run example:** `CUDA_VISIBLE_DEVICES=0 "$PY" tslm_study/run_tsqa.py --model mamba --llm-id state-spaces/mamba-1.4b-hf --lora-r 0 --encoder quant --train-n 8000 --epochs 3 --bs 8 --test-n 2000 --save checkpoints/foo.pt`
- **Recipe (quant @ scale) = LoRA WINS: `--lora-r 16` + trainable bin-embeds, lr ~1e-4/2e-4.** Beats full-FT (0.936 > 0.914 @1.4b TSQA), lr-robust (full-FT crashes @5e-5 → needs ≤2e-5; LoRA stable 5e-5→2e-4, only breaks @5e-4), 12× cheaper (118M vs 1373M trainable). bf16 fine (nan was padding). When `lora_r>0`+`quant`, MambaTSLM auto-keeps embeds+lm_head trainable so the new tokens learn. Full-FT fallback exists (lr ≤2e-5 @1.4b).
- **Checkpoints → `OpenTSLMExperiments/checkpoints/` (durable, NOT /tmp).** `--ckpt PATH` = eval-only reload (validated exact round-trip). **Run records → `tslm_study/runs/*.yaml`** (config+metrics, every run).
- **Kernels:** `mamba-ssm 2.3.2` + `causal-conv1d` installed (fast fused scan; full-FT 370m = 4.1GB / 0.30 s-step vs slow-path OOM/14.5s). `OpenTSLMSP` (transformer baseline) runs via `--model sp --llm-id meta-llama/Llama-3.2-1B`.
- **DISTRIBUTED (data-parallel) = `CUDA_VISIBLE_DEVICES=... bash tslm_study/ddp.sh <nproc> <script.py> [args]`.** Manual-DDP (`tslm_study/dist_utils.py`: broadcast init + all-reduce grads; rank-0 logs/saves/evals) wired into `run_pretrain.py` (each rank = own KernelSynth shard, eff_bs = nproc×bs). **NCCL was BROKEN here — 3 stacked issues (debugged 2026-06-06):** (1) torch 2.9's bundled **NCCL 2.27.5 HARD-CRASHES on the first collective** (A100 + driver-580/CUDA-13; silent exit/segv, no error) → FIX = **NCCL 2.30.4 LD_PRELOAD'd from `vendor/nccl230/libnccl.so.2`** (needs the `ncclCommWindowRegister` symbol → can't go <2.27). (2) box's **gIB NCCL net plugin is for H100-class (a3u/a4)** but GPUs are A100 → `NCCL_NET=Socket`. (3) **NVLS** (NVLink-SHARP) is H100+ → `NCCL_NVLS_ENABLE=0`. All baked into `ddp.sh`. (gloo works via `TSLM_BACKEND=gloo` but CPU-staged = slow.)

## 2. Carry-forward facts from the archived era (still useful later)
- **CiK forecasting harness EXISTS** at `context_forecasting/cik/` (conda env `cik` = `/home/tonyy/miniconda3/envs/cik/bin/python`; `cik_benchmark/` with the 71-task suite + RCRPS). Reuse it when we build the forecasting-output regime (#3). Plug in via a `cik_benchmark.baselines.base.Baseline` subclass (`__call__(task, n_samples) -> samples`). Verified competitor RCRPS bars are in [[ref-target-leaderboard]] (NOTE: old journal quoted 0.143/0.298 for CiK — those did NOT verify; use the leaderboard memory).
- **OpenTSLM essentials:** `OpenTSLMSP.py` / `OpenTSLMFlamingo.py`; datasets in `src/opentslm/time_series_datasets/{har_cot,sleep,ecg_qa,TSQADataset}` (unified dict: `pre_prompt/time_series_text/time_series/post_prompt/answer`); `extend_time_series_to_match_patch_size_and_aggregate` = the shared collate.

## 3. LOG (SSM era)
### 2026-06-05 — SSM pivot + infra (this session)
- Pivoted to fully-SSM Mamba TS-LM. Built + validated `MambaTSLM`. Fixed the cuBLASLt env crash (`tslm_env.sh`).
- Installed mamba-ssm fast kernels (huge memory+speed win). Root-caused the training `nan` = fused kernel backward on **left-padded** positions → fixed with batched right-pad (8× faster, GPUs saturated).
- Added checkpoint save/load (validated) + per-run YAML log. Confirmed **full-FT > LoRA**.
- **HAR-CoT representation bake-off:** vectors ≈ floats ≈ quant ≈ quant+bidir, ALL ~0.50 acc (n=2000) → HAR too noisy/hard to discriminate representations.
- Ran a **6-agent design-space recon** (TS-tokenization, SSM-arch, SSM-vision/audio, VLM-transfer, training, regimes) → ranked idea backlog [[project-idea-backlog]]. Key competitive finding: TsLLM already does interleaved TS+text gen but with encoder+MLP-head+transformer → our open cell = encoder-free+head-free+SSM.
- Base-model recon: small `state-spaces/mamba` is a weak base; no strong pure-SSM at 1-3B; plan Falcon-7b/Zamba2 escalation [[ref-base-model-options]].
- **TSQA breakthrough (user's hypothesis):** on the clean 4-way TSQA, representation SEPARATES — **quant 0.926 > linear 0.860 (~7σ @ n2000)**, gap on value-precision tasks (Outliers/Volatility). → DECIDED quant; TSQA = the sensitive testbed [[project-findings-representation]].
- Multi-seed variance check built (`--seed`) but PARKED; bidir-on-TSQA queued (re-test on a task that can see it).

### 2026-06-06 — quant-1.4b scaling saga (representation re-opened, then resolving)
- TSQA-370m 3-way settled: **floats 0.9355 ≈ quant 0.926 > linear 0.860** (pure-token > encoder, value-precision).
- **Scaling to 1.4b broke quant:** quant-1.4b @lr5e-5 = **0.8055** (WORSE than 370m, and < linear-1.4b 0.920); linear scaled normally (0.86→0.92). Not noise (~12σ). Root cause = the new bin tokens destabilized by too-hot full-FT lr (early loss spike 2.71 vs train-loss-recovers-but-generalizes-worse). **floats-1.4b OOM'd** even @bs4 (long digit seqs) → floats ruled out; quant compactness is *necessary*.
- **Two fixes under test:** (1) lower lr — quant-1.4b @lr2e-5/1e-5 train STABLY (loss 0.04/0.009, no spike) [results pending]; (2) **LoRA + trainable bin-embeddings** — built & smoke-passed (embeds trainable, LoRA present, bin tokens get grads). User's fairness note: must also lr-sweep linear-1.4b → compare best-vs-best.
- **Next once recipe locked:** stop rep ablations → PIVOT to the contribution (regimes): forecasting-output→CiK (tractable, harness exists) or long-signal/iso-compute (cleanest structural win + pulls in hierarchical-VQ).

- **LoRA ablation — round 1 LOST, must re-run fresh.** LoRA-quant-1.4b (r=16, trainable bin-embeds, 117.9M) lr-sweep {5e-5,1e-4,2e-4,5e-4} vs full-FT best 0.914. Qualitative win seen before death: **lr5e-5 trained STABLY where full-FT crashed → LoRA frozen-backbone is lr-robust.** BUT: stopping the chaining watcher (to cancel round-2) via TaskStop ALSO killed round-1's children at step2500/3000 — *before* `--save` → **no ckpts written.** **OPS LESSON: `TaskStop` on a watcher kills its child training procs too (process group), does NOT orphan them — to cancel a chained watcher while keeping a child alive, `kill` the watcher's bash PID specifically, or don't chain.** **POST-RESTART:** `source tslm_env.sh`; run LoRA-quant lr-sweep FRESH as STANDALONE bg runs (no chained watcher). **quant DECIDED regardless** — LoRA only picks the training recipe (full-FT@2e-5 works); LoRA is LOW-PRIORITY vs pivoting to the regime.

### 2026-06-06 (cont.) — SIGNAL-OUTPUT capability built + validated (the "out" half of pure-token in/out)
- Server upgraded to **4× A100-40GB** (was 2). User chose "build signal-output first" (regime-agnostic foundation).
- Added to `MambaTSLM`: `_quantize_ids`/`_dequantize` (vocab↔center lookup), `forecast_loss` (teacher-forced LM loss on FUTURE value-bin tokens), `forecast` (bin-CONSTRAINED generation via a logits processor → dequant → un-z-score; history & future share the history's z-score stats). **No forecasting head** — the future is just more bin tokens the SSM continues the stream with.
- CPU smoke PASSED: forecast_loss finite + backprops; `forecast` emits finite right-shape values; **40-step overfit drops forecast MSE 1.01 → 0.00** on a sine task → the SSM genuinely learns to GENERATE signal. → forecasting/generative regimes unlocked.
- **VALIDATED at scale (synthetic held-out, `run_forecast.py`): model BEATS all baselines** — MSE 370m **0.32** / 1.4b **0.22** vs naive 0.70 / linear 0.54 / mean 0.88; beats naive on 77%(370m)/83%(1.4b) of series. **Forecasting IMPROVES with scale** (0.32→0.22) — unlike input-classification where quant≈linear tied @1.4b. So the OUTPUT side has real scale headroom. Signal-output works end-to-end.
- **NEXT — wire CiK** (`context_forecasting/cik/`, conda env `cik`): the headline = context-aided forecasting. Crux = CiK is TEST-only → need a CONTEXT-FUSION training corpus (synthetic hard-context: stated caps/spikes/trends the model must USE) → train mamba-quant to fuse context-text + history → zero-shot RCRPS vs the prompted-LLM frontier ([[ref-target-leaderboard]]). Plug in via `cik_benchmark.baselines.base.Baseline` subclass wrapping `.forecast`.

### 2026-06-06 (cont.) — CONTEXT-USE PROVEN (the CiK-thesis capability) + LoRA wins output too
- **Forecast study:** LoRA beats full-FT for OUTPUT too (370m 0.234<0.32, 1.4b 0.182<0.22) → **LoRA r=16 = unified recipe (input AND output).** Horizon-48 harder (0.45/0.34) but still beats naive 77-85%.
- **CONTEXT-USE PROOF (`run_ctx_forecast.py`):** future trend (up/down/flat) set by a context sentence, NOT derivable from history. **context-ON vs OFF MSE: 370m 0.10 vs 0.66 (−85%), 1.4b 0.09 vs 0.65 (−86%)** → the SSM genuinely FUSES language context with the signal to forecast. The CiK-thesis capability is real.
- **STATE: thesis fully load-bearing — signal in ✓ (TSQA), signal out ✓ (forecast beats baselines + scales), context fusion ✓.** Pure-token SSM does context-aided forecasting (winnable vs text-blind TSFMs).
- **NEXT — real CiK:** CiK is test-only → train a context-forecaster on a corpus (richer synthetic hard-context: caps/spikes/shifts/numeric, and/or real forecasting data) → plug into `context_forecasting/cik/` via a `Baseline` subclass wrapping `.forecast` → zero-shot **RCRPS vs the prompted-LLM frontier** ([[ref-target-leaderboard]]). Numeric-magnitude context (vs categorical) is the open design point (scale-consistency of stated values vs z-scored signal).

### 2026-06-06 (cont.) — ctx-rich ablation: per-event-type fusion + robustness (EXPERIMENTATION phase, user steer: ablate-to-understand, don't build/grid)
- **Robustness:** context-fusion holds across horizon-48 (+83%), more-data-16k (+87%), full-FT (+83%) — stable, not a fragile artifact.
- **Per-event ON/OFF (1.4b LoRA, richer 4-event corpus):** trend **+78%**, shift **+81%** (SUSTAINED/directional context fuses WELL); **spike −51% — context HURTS** (precise single-point event: attempt-and-miss > ignore); none +10% (clean control — generic context doesn't hallucinate events).
- **Spike is FRAGILE/model-dependent** (370m +79%, full-FT +44%, 1.4b-LoRA −51%, h48 +20%). **INSIGHT: the SSM fuses sustained/directional context reliably; precise POINT-EVENT context is hard.** Design implication for CiK: expect good fusion on sustained scenarios (heat wave, trend), weaker on precise events. (Possible fixes to ablate later: longer/repeated event encoding, or a magnitude-aware context.)
- **Running (bpru3h34j):** generalization ablation (held-out event type → does fusion generalize vs memorize) + CiK transfer probe (synthetic model on 9 real CiK tasks, ON vs OFF).

### 2026-06-06 (cont.) — 6-agent research sweep + de-risking ablations (2 of 3 big bets VALIDATED)
- Research sweep → design spec [[project-ctx-forecast-design]] (5 levers). Then built + ran ablations to de-risk the biggest bets BEFORE finalizing the corpus.
- **BET #2 (generalization) VALIDATED** (`run_ctx_comp.py`, 370m): factor context into orthogonal primitives [shape×dir×mag], train 8 combos, HOLD OUT 4 novel combos. **Held-out novel combos +94% (ON 0.31 vs OFF 5.65) ≈ in-dist +95%** → the model COMPOSES seen primitives and generalizes to unseen COMBINATIONS. Flips the earlier scenario-memorizing +0%. **The fix = combinatorial coverage + hold out COMBINATIONS not PRIMITIVES.** (1.4b confirmation in flight.)
- **BET #1 (constraint masking) VALIDATED** (`run_ctx_cap.py` + `forecast(caps=)`): mask out-of-bound bins at decode → **violations 14.0% → 0.0% at ZERO MSE cost** (0.0131→0.0132). Free structural win vs the β=10 RCRPS penalty; an edge Gaussian-head TSFMs can't match. (Smoke-validated; code in MambaTSLM.forecast.)
- **BET #2 confirmed @1.4b:** held-out novel combos **+89%** (ON 0.40 vs OFF 3.62), all shapes incl spike +88%. Generalizes at both scales.
- **BET #3 (numeric phrasing) VALIDATED:** stating the magnitude FIXES the spike (ctx-rich spike −51% → here **+88–90%**) — the earlier failure was UNSPECIFIED magnitude, not a fusion limit. And **σ-precise > vague, esp. on held-out** (spike held-out +88% precise vs +77% vague). → pre-convert numbers to σ-units in the pipeline.
- **ALL 3 CORPUS-DESIGN BETS VALIDATED. Experimentation phase COMPLETE.** Evidence-backed spec: primitive-factored + combinatorially-covered (hold out COMBINATIONS) + σ-precise numeric context + constraint-mask decoding. Remaining to build: real-data numeric pretrain (#5) + distance-aware loss (#4, deferred) + the CiK-matched corpus generator + full-71 eval. [[project-ctx-forecast-design]]

### 2026-06-06 (cont.) — BUILD phase started (server restart pending)
- **CiK-matched corpus model BUILT + SAVED** (`run_ctx_train.py` → `checkpoints/ctx_cikmatched_{14,370}.pt`): factored shape{trend/shift/spike/BOUND}×dir×mag, σ-precise numbers, domain framing + distractors + verbosity, combinatorial coverage. Held-out generalization: 370m +87% (clean); 1.4b +24% (muddied — good base forecaster = low OFF headroom + the `bound-up-large` held-out combo barely binds). Constraint: 1.4b learned the bound (0% violations unmasked); 370m 0.8%→0% with mask. → generalizes + respects constraints; **real test = full-71 CiK.**
- **Distance-aware loss ablation (bet #4) — NEGATIVE, CE WINS:** plain-CE CRPS 0.177/MSE 0.127 vs soft-ordinal(σ=1) CRPS 0.202/MSE 0.157 (soft HURT both). With 256 FINE bins, spreading target mass just blurs precision (distance-aware helps COARSE bins). → **stick with plain CE.** (`soft_sigma` impl kept in forecast_loss for the coarse-bin case.)
- **Full-71 HONEST BASELINE (142 instances / 71 tasks): ctx_rich_14 RCRPS 0.454 — WORSE than naive (0.379), far above TSFM bar 0.326; context only +4%.** Blunt: the synthetic-trained model is a POOR real forecaster (worse than repeat-last). Confirms the weak-base diagnosis — **numeric-competence PRETRAINING (#5) on real data is the DOMINANT missing piece**, not just context. The +85% synthetic context-fusion + validated tricks do NOT yet translate to real data (bad base forecaster + synthetic≠real context). This is the honest starting line; need <0.326 to beat TSFMs.
- **⛔ POST-RESTART RECOVERY:** `source tslm_env.sh`. Durable + safe: corpus checkpoints saved, all design findings in [[project-ctx-forecast-design]] + TRICKS. In-flight eval runs (CE/soft, baseline) are re-runnable (no ckpt). **NEXT after restart:** (1) run CiK-matched full-71 eval — `cik_forecast.py <ckpt> 20 <out.npz>` (path-parametrized) then `cik_eval.py <out.npz>` in the cik env — compare ctx_cikmatched_14 vs ctx_rich_14 (+8%); (2) fold the winning loss; (3) numeric-competence pretraining on real data (the last big lever, #5).

### 2026-06-06 (cont.) — CiK-matched corpus model full-71 + pretraining launched
- **CiK-matched corpus model (ctx_cikmatched_14) full-71: ON 0.396 / OFF 0.374 / naive 0.379 (context HURTS −6%).** vs baseline ctx_rich (ON 0.454/OFF 0.471). TWO findings: (1) **base forecaster IMPROVED** (OFF 0.471→0.374 ≈ naive — richer/factored synthetic forecasting helped); (2) **context MISFIRES on real CiK (−6%)** — synthetic-trained σ-precise factored context gets mis-applied to real scenarios (heat wave ≠ "2σ spike at step k"), worse than ignoring. Synthetic→real context gap = active misfiring, not just weak transfer.
- **IMPLICATIONS:** (a) numeric PRETRAINING still essential (base only ≈naive, need <0.326); (b) context must be a LIGHT correction on a STRONG base (CorDP framing, research agent 1) and/or trained to match REAL CiK phrasing — not allowed to dominate. The "context can hurt" failure mode wasn't visible on synthetic (where context always matched).
- **KernelSynth pretraining RUNNING** (4 variants: fast-3k/LoRA-r64-6k/full-FT/370m) — the dominant lever to make a competent base forecaster. Then context fine-tune (CorDP-style) on top.

### 2026-06-06 (cont.) — KernelSynth pretraining WORKS (beats naive); pivot to Falcon-H1 base
- **Base-forecaster progression on full-71 CiK (context-free OFF, lower=better):** ctx_rich(sines) **0.471** (worse than naive) → ctx_cikmatched(richer factored synth) **0.374** (≈naive) → **KS-pretrain-6k (diverse GP-kernel synth, LoRA r64) 0.369 — BEATS naive 0.379!** KS pretraining (the dominant lever) made a competent-ish forecaster. (3k/370m/full-FT re-evaluating; full-FT best on KS-heldout 72%.)
- **BUT still short of TSFM bar 0.326** — on the WEAK Pile-mamba-1.4b base this may be the ceiling.
- **PIVOT → `tiiuae/Falcon-H1-1.5B-Deep-Base`** (user-suggested; strong instruction-tuned mostly-SSM hybrid; [[ref-base-model-options]]). Hypothesis: strong base pushes past TSFM AND fixes context-MISFIRE (real language parses "heat wave"). Integration started (load + LoRA-target inspect); quant/forecast machinery is base-agnostic, LoRA targets need retune for the hybrid.

### 2026-06-07 — BENCHMARK vs published OpenTSLM (we ARE OpenTSLM-Mamba); FH1 + bidir both NEGATIVE
- **MAJOR PIVOT (user):** stop internal ablations → benchmark our fully-SSM Mamba against PUBLISHED OpenTSLM (paper arXiv 2510.02410). We = **"OpenTSLM-Mamba"**. Baselines = **OpenTSLM-SoftPrompt** (frozen Llama-3.2-1B + TransformerCNN encoder + MLP projector, soft-prompt) + **OpenTSLM-Flamingo** (cross-attn). 5-stage curriculum: TSQA-mcq → M4-captioning → HAR-CoT → Sleep-CoT → ECG-QA-CoT. Targets: **HAR 65.4, Sleep 69.9 F1, ECG>GPT4o**.
- **MambaTSLM WIRED into `curriculum_learning.py`** (6 edits: parser/init/optimizer/save/load/_load_previous_stage) + smoke-verified (HAR-CoT compute_loss finite, ckpt roundtrip; NO interface adaptation — CoT batch matches MambaTSLM._prompt_embed, same path as run_har).
- **Config (durable in repo):** stages **1-4 only** (ECG dropped, too long), epochs **HALVED → 15/10/15/30** (edited `num_epochs=` in curriculum_learning.py). bs4/device. LoRA r16 (SP enables stage3+; Mamba throughout — MATCHED on CoT stages). lr: SP enc2e-4/proj+lora1e-4; Mamba 2e-4. FAIR (schedule+batch identical all; lr architecture-appropriate). CoT datasets auto-download from HF (cached). [[feedback-keep-gpus-busy]]
- **FH1 (Falcon-H1 hybrid) DROPPED — forecast = GENERATION BUG:** teacher-forced loss = mamba's (2.58≈2.61, it LEARNED) but autoregressive rollout broken (KS-MSE 1.34 vs mamba 0.108; CiK 0.61 worse-than-naive). TSQA 0.871 < mamba 0.939. The hybrid's attention/cache breaks pure-token autoregressive GENERATION; pure-SSM generates cleanly → **REINFORCES fully-SSM thesis.** (Falcon3-Mamba-7B = pure-SSM scale-up base if needed.)
- **BIDIRECTIONAL = NEGATIVE (concluded):** TSQA Mamba-1.4b, unidir 0.940/0.943 (avg 0.9415) vs bidir 0.945/0.9395 (avg 0.9423) — +0.08pt = WITHIN seed noise (0.3-0.55pt), inconsistent (wins s0 loses s1), + 1.7× compute (2× tokens; SSM compute is O(L) LINEAR, "constant" only = per-step-gen O(1) + inference state-mem). → **DROP bidir, use UNIDIRECTIONAL.**
- **★ RECOVERY / NEXT (post server-restart — user paused to restart):** launch the **4x-DDP OpenTSLM benchmark**, per model SEQUENTIAL (no idle): `CUDA_VISIBLE_DEVICES=0,1,2,3 bash tslm_study/ddp.sh 4 curriculum_learning.py --model <M> --llm_id <id> --device cuda --stages stage1_mcq stage2_captioning stage3_cot stage4_sleep_cot`. Models: **OpenTSLMSP**(meta-llama/Llama-3.2-1B), **OpenTSLMFlamingo**(Llama-3.2-1B), **MambaTSLM**(state-spaces/mamba-1.4b-hf, UNIDIR), **MambaTSLM**(mamba-370m). Trainer DDP verified OK (reads torchrun env, set_device per rank, 999h timeout=no cleanup-NCCL-crash). eff-bs-16; same-for-all=fair. NCCL via ddp.sh (2.30.4+Socket+NVLS). **cik_eval must be SERIAL** (HDF5 race). Report HAR/Sleep head-to-head. Results land in `results/{llm}/{model}/{stage}/results/metrics.json`.

### 2026-06-09 — REP-AUDIT closed → THE BIG REFACTOR (clean OpenTSLM+Mamba) → Flamingo fixed → official-TSQA setup
**Session arc:** finished the rep audit → diagnosed the SP "NaN" → first real head-to-head (Mamba-370m partial BEATS SP) → **major engineering: refactored the whole repo into a clean "OpenTSLM + Mamba feature" on a fresh fork of upstream** → fixed upstream's broken Flamingo → evolved the training schedule (epochs/patience/min_delta) → **found a LoRA confound in the TSQA head-to-head** → set up + launched official TSQA (stopped mid-run, rerun pending).

#### A. Pre-benchmark REP-AUDIT — COMPLETE (TSQA-370m, zscore, 256 bins, paired seeds {0,1}, train-n 8000)
Baseline uniform/numeracy/ep3 = 0.9415/0.9540 (mean 0.948).
- **(1) bins — uniform > quantile:** quantile 0.9315/0.9230 (0.927) → uniform wins **+2pt** both seeds (equal-mass over-concentrates near 0 + collapses tails into 2 coarse bins; near-identical central centers hard to distinguish). **keep uniform.**
- **(2) init — numeracy > random (REAL +3.9pt):** random 0.9030/0.9155 (0.909) vs numeracy 0.948. Seeding bin tokens from the LLM's number-text embedding = genuine sample-efficiency. **keep numeracy.**
- **(3) epochs 6>3 +2.8pt:** ep6 0.9795/0.9725 (0.976). Undertrained at ep3 — but steep part of the curve (3→6); 15→30 expected flat.
- **VERDICT (locked): quant + zscore + 256 UNIFORM bins + NUMERACY init.** Training-EFFICIENCY dims (init, epochs) matter; pure-shape dims (scaling/vrange/soft-CE) rep-agnostic at scale. Banked: "quant tokenization is normalization-agnostic." (Audit batch killed externally mid-run TWICE — recurring on this box; wave-1 survived on disk, wave-2 relaunched.)

#### B. SP "NaN" — COSMETIC, not training divergence
SP trained CLEAN (15/15, 0 skips, loss→0.0014) yet metrics.json showed test_loss=NaN. Root cause = the curriculum's **test_loss metric NaNs on some batch**, but the **generated predictions are SANE** (SP TSQA acc 0.836). → **ALWAYS compute acc/F1 FROM test_predictions.jsonl, never from test_loss.** Added a DDP-safe non-finite-loss guard (all-reduce MIN so all ranks skip together — per-rank skip deadlocks the grad all-reduce); **later REMOVED in the clean integration** (not Mamba-related, unrelated change to upstream's loop).

#### C. FIRST head-to-head (partial): Mamba-370m BEATS SP
Mamba-370m (ep3 PARTIAL, 2000-subset) TSQA **0.9185** vs SP (full) **0.836** = **+8.2pt**. Per-task Mamba **Trend 1.000 / Out 0.996 / Vol 0.992** vs SP 0.940/0.886/0.879; **Seasonality tied ~0.66** (shared hard task). Gap concentrated on VALUE-PRECISION tasks (quant preserves values, soft-prompt encoder averages them).

#### D. main_results/ — RESULTS.md + config.yaml + checkpoints/ + predictions/ + archive.sh (the official-results home).

#### E. ★★★ THE BIG REFACTOR — clean "OpenTSLM + Mamba feature" (user: "looks like OpenTSLM + mamba feature, NOT mamba codebase + opentslm foundation")
- Fetched upstream **`StanfordBDHG/OpenTSLM` @104013b**. Our fork was 7 ahead (AHRI-era) / 2 behind; all Mamba+experiment work UNCOMMITTED. Created clean branch **`mamba-integration` off upstream/main** (ahri-era edits stashed on `ahri-signal-output`).
- **PR1 (72cfed7) MambaTSLM model:** cleaned 364→314 lines — DROPPED PatchSignalEncoder(linear/mlp/conv)+floats+bidir (rejected reps); KEPT quant path + forecast()/forecast_loss() signal-OUTPUT + quant sub-knobs (scaling/bin_mode/init_mode params). Added `[mamba]` OPTIONAL extra (mamba-ssm+causal-conv1d). Full TimeSeriesLLM interface.
- **PR2 (a47d0c5) curriculum:** `--model MambaTSLM` into 5 branches (_initialize_model/_get_optimizer[single LR group]/_save/_load_checkpoint/_load_previous_stage) + argparse. **+73/−1 PROVABLY ADDITIVE** (only `−` = argparse line; SP/Flamingo byte-identical). Lazy import (optional mamba-ssm only when selected).
- **PR3 (d761922) experiments/:** curated harnesses (run_tsqa/run_har/run_pretrain + generic ddp.sh + dist_utils) adapted to quant-only (dropped --encoder/--bidir, kept scaling/bin/init) + README. Box-specific NCCL/tslm_env stays UNTRACKED (tslm_study/).
- **VERIFICATION all green:** ★ **REGRESSION BIT-EXACT** — cleaned model + ep3 ckpt reproduces TSQA **0.9185 identical per-task** (zero behavior change, strongest no-bug proof). + smoke (build 58.9M/train/save-load strict=False 482 frozen keys/generate) + `test_curriculum_trainer` **8 passed** + curriculum+Mamba-130m trains end-to-end. The 12 pytest-collection errors = pre-existing upstream + untracked cik/, NOT ours.

#### F. Flamingo was BROKEN on upstream — FIXED (87837c9), 3 bugs
open_flamingo exposes `vision_encoder` as a `types.SimpleNamespace` stub (OpenTSLM uses Flamingo for TS, no real vision backbone). Ported ahri-era fixes: (1) **__init__** requires_grad_ on SimpleNamespace → `hasattr` guard + `.visual` fallback; (2) **forward** _encode_vision_x `vision_encoder(x)` not callable → unwrap `.visual`; (3) **device** SimpleNamespace-wrapped encoder not moved by `.to()` → `encoder.to(vision_x.device)`. Verified forward+backward OK (loss 1.92, 837.7M trainable).

#### G. Training-schedule evolution (config 1d98b61)
- Epochs: upstream **30/20/30/60/60** → halved 15/10/15/30 → "all 10" → **reverted to upstream 30/20/30/60/60** (generous CEILINGS). Patience upstream **5 → 3**.
- **min_delta 1e-4 → 0.01** (`avg_val_loss + 0.01 < best_val_loss`). Precedent = Keras/Lightning min_delta. Compares to the (frozen-during-dry-spell) BEST so improvements ACCUMULATE → "stop if you can't shave 0.01 off the best within a rolling 3-epoch window." 0.004/ep SURVIVES, 0.003/ep dies. NOT "0.01 every epoch."
- Per-epoch train+val saved to loss_history.txt → convergence graph + "epochs-to-convergence" efficiency column.

#### H. ★ LoRA CONFOUND found (TSQA head-to-head = apples-to-oranges on adaptation) — STILL OPEN
Curriculum gates LoRA to **stages 3+** (`stages_with_lora=[stage3/4/5]`). So on **stage1 (TSQA)+stage2 SP/Flamingo are FROZEN-backbone** (encoder/projector only). **Mamba's LoRA r16 is baked into __init__, never gated → ON from stage1.** → On TSQA Mamba adapts its backbone, SP/Flamingo can't → explains SP's slow convergence AND is a CONFOUND (part of Mamba's edge is the LoRA, not just the SSM). Options: (1) match capacity (SP LoRA on stage1) or (2) each-at-intended-protocol + report. User chose epochs+min_delta, NOT the LoRA change → current run = "each at intended protocol."

#### I. Epoch/undertraining finding (why 30-ep + min_delta)
SP @10 epochs = **UNDERTRAINED** (val still dropping ep10 0.501→0.457, best=last, never early-stopped) → TSQA **0.621** vs the 15-ep **0.836**. SP's published LRs are tuned for the 30-ep schedule. Mamba converges ~ep2-3 → 10 is plenty for IT. A fixed 10-cap **handicaps the slow models, not Mamba** (reviewer: "you crippled the baseline"). Fix = 30-ep CEILING + min_delta → each trains to convergence (Mamba ~ep6, SP/Flamingo ~15-20).

#### J. Official TSQA — set up, attempted, STOPPED (rerun pending)
Cleared old dirs (0.919 preview → `results_preview_20260609/`). Attempt-1 (parallel single-GPU bs8) WRONG → user "4-way distributed local bs4 like before"; also surfaced the Flamingo crash. Attempt-2 (correct) = `tslm_study/run_official_tsqa.sh` = 4 models SEQUENTIAL via `ddp.sh 4` (local bs4 → **eff-16**), --stages stage1_mcq, 30-ep+min_delta. SP reached ep5 then **run stopped** (external kill AGAIN — recurring). No completed results.
- **RERUN:** `bash tslm_study/run_official_tsqa.sh` (SP/Flamingo[fixed]/Mamba-1.4b/Mamba-370m, sequential 4xDDP eff-16). Each train→save→auto-eval→predictions. Acc FROM predictions. Archive predictions+ckpts+loss_histories to main_results/.

#### K. Runtime + compute reality
- **TSQA ~13-20h** sequential (measured SP ~6min/ep @4xDDP: SP 2-4h, Flamingo 4-8h heavier-837M, Mamba-1.4b 4-5h, Mamba-370m 2.5-3.5h).
- **Later stages multi-day** (CoT stages = longer seqs + slow generation eval; Sleep 60-ep ceiling = long pole). Full 4×4 grid ≈ **3-5 days.** User datapoint: **Flamingo+full-ECG ≈ 7 days on 4×A6000** (our 4×A100-40GB ≈ similar). ECG rightly dropped.
- **EFFICIENCY ANGLE (reportable):** Mamba ~6 epochs vs Flamingo ~60 → "Flamingo-level in ~10× fewer epochs / fraction of wall-clock" — reframes more-tokens/memory critique as throughput-to-convergence.
- **ECG memory (forward):** loader downsamples 5× (500→100Hz) → ~1000/lead × 6-12 leads = **~6-12k quant tokens**. SSM O(L) makes it feasible (transformer O(L²) dies); bs4/12-lead OOMs → needs grad-checkpointing (NOT yet wired for Mamba in curriculum) + bs1-2. We use MORE memory than encoder-compressing SP/Flamingo: trade = memory-for-resolution, NOT a memory win.

#### L. RERUN RELAUNCHED (2026-06-09 19:24)
- `bash tslm_study/run_official_tsqa.sh` relaunched (nohup, pid 64261; driver log `logs/official/driver.log`). SP **resumes from the ep5 checkpoint** (trainer loads best_model.pt + optimizer/scheduler, appends loss_history — verified resume path in curriculum_learning.py before launch). Order: SP → Flamingo(fixed) → Mamba-1.4b → Mamba-370m, each 4xDDP eff-bs16, 30-ep ceiling + patience 3 + min_delta 0.01. ETA ~13-20h total.

#### M. RECIPE-OPTIMIZATION PASS (Claude-5 review of the pipeline, 2026-06-09 evening) — wave QUEUED
User asked for a fresh-eyes optimization pass before the head-to-heads; approved Tier-1 + Tier-2 + patch-fix. **All implemented WITHOUT touching core files** (official run imports them live): subclass-based harness `tslm_study/run_tsqa_ablate.py` (untracked). Winners get folded into `MambaTSLM.py`/curriculum as clean commits afterwards.
- **Code findings from the review (Claude-4 blind spots):**
  1. `compute_loss` masks the WHOLE prompt to -100 → the input signal tokens carry free dense labels (their own next-bin ids) that go unused. SP/Flamingo structurally CAN'T have this (no token targets) → thesis-native lever. → `--aux-weight`.
  2. LoRA path sets `requires_grad=True` on the ENTIRE embedding matrix (tied lm_head, verified `tie_word_embeddings=True`) → (a) all ~50k text rows fine-tune at 2e-4 (most of the "118M trainable"), (b) AdamW's decoupled wd=1e-2 shrinks ALL rows every step (~e^-lr·wd·steps). → `--embed-wd 0`, `--embed-grad-mask` (bin-rows-only).
  3. Single LR group for fresh-init bin rows + LoRA adapters. → `--embed-lr` split.
  4. Collate pads series with raw 0.0 to patch_size=4 multiples → fake values in Mamba's token stream + distorted z-stats. NO-OP on TSQA (lens 64/128/256/512), real on M4/Sleep. → staged patch `tslm_study/staged_patches/patchsize1_mamba.patch` (apply AFTER official run; dry-run verified).
  5. Curriculum instantiates MambaTSLM at fp32 default (harness used bf16) — throughput, not correctness; not changed mid-run.
- **Tier-2:** (a) warm-start from `checkpoints/pretrain_ks_370.pt` (it's r64 → load@r64 → `merge_and_unload` → fresh r16 wrap = capacity-identical finetune); (b) **Mamba-2 backbones** `AntonV/mamba2-370m-hf` (SAME-SCALE paired vs audit baseline!) + `mamba2-1.3b-hf` (cached). peft rejects out_proj for mamba2 → LoRA targets `["in_proj"]` only.
- **SMOKES ALL GREEN:** aux-path main CE == parent loss BIT-EXACT (4.193064); grad-mask: non-bin-row grad max 0.00e+00; mamba2 train+generate OK (55.9M trainable); warm-start r64 missing=0/unexpected=0 → merge → r16 (288 lora tensors ✓); full CLI run OK.
- **WAVE (queued):** `tslm_study/run_ablate_wave.sh` — 10 arms × paired seeds {0,1}, audit protocol exactly (370m lora16 quant/zscore/u256/numeracy, train8000 bs8 lr1e-4 ep3 test2000): A0 replication-guard (expect ≈0.9415/0.9540) · A1/A2 aux 0.1/0.5 · B1 wd0 · B2 grad-mask+wd0 · C1/C2 embed-lr 5e-4/1e-3 · D1 mamba2-370m · D2 mamba2-1.3b · E1 warm-start. ~20 runs ≈ 5-7h on 4 GPUs.
- **ORCHESTRATION:** `tslm_study/babysit_then_wave.sh` (nohup, RUNNING) — waits for official TSQA; if it's killed externally AGAIN, relaunches it (trainer resumes); on genuine completion auto-fires the wave. Logs: `logs/official/babysit.log`, `logs/ablate/wave.log` + per-arm `logs/ablate/<tag>.log`; YAML records `tslm_study/runs/*TSQAabl*`.
- **DECISION RULE (when results land):** adopt a knob if it wins on BOTH seeds and the paired mean improves ≥ the audit's noise bar (~0.5pt); ceiling caution — baseline is ~0.95, so judge aux/warm-start also on Seasonality sub-task (the 0.66 headroom task) + loss curves. Then: apply staged patch + fold winners into MambaTSLM/curriculum + rerun ONLY the two Mamba TSQA legs with the locked recipe.

#### N. RE-SEQUENCED (user, 2026-06-09 20:00): ideas FIRST, tables AFTER — official run KILLED, wave launched NOW
- User: recipe ideas should be tested BEFORE the official head-to-head tables, **and resume-from-checkpoint is NOT trusted — any stopped run restarts from scratch** (durable rule, saved to memory `feedback-no-resume-restart-fresh`).
- Killed the official TSQA run (SP was at ep10/30, val 0.505) + the babysitter (it would have relaunched). Partial SP state QUARANTINED → `results_partial_killed_20260609/` (prevents any accidental auto-resume; the ep5-resumed checkpoint is tainted by the no-resume rule anyway).
- **Ablation wave launched directly 20:02** (pid 67777, `logs/ablate/wave.log`, first arms D2_m2-13 s0/s1 + A0_base s0/s1 on GPUs 0-3). ~5-7h.
- **NEW SEQUENCE:** wave → adopt winners (decision rule in §M) → apply staged patch + fold winners into core → THEN the official 4-model TSQA tables from scratch (SP rerun included — its partial numbers are void), with the locked recipe.

#### O. WAVE-2F: FORECAST-side ablation CHAINED (user: "test the ideas on forecasting too — we should do both")
- New harness `tslm_study/run_pretrain_ablate.py` (reuses MambaTSLMAblate + experiments/run_pretrain's kernelsynth + PAIRED held-out eval, n=500 fixed test seed; single-GPU per arm) — smoke OK.
- **Wave-2F arms** (`tslm_study/run_forecast_wave.sh`, paired seeds {0,1}, 370m lora16 lr2e-4, 3000 steps bs16, KS-MSE vs naive): F0 fresh baseline · FB1 embed-wd0 · FB2 grad-mask+wd0 · FC1 embed-lr 5e-4 · **FS1/FS2 soft-sigma 1.0/2.0** (distance-aware CE — implemented 06-06, never A/B'd) · FD1 mamba2-370m. aux-loss N/A (forecast loss is already dense next-bin), warm-start N/A (this IS the pretrain). Ckpts → `checkpoints/ablateF/` (winners get the CiK real-data eval later; cik_eval is SERIAL-only, HDF5 race).
- **Chainer `tslm_study/chain_waves.sh` (pid 70313, RUNNING):** waits for wave-1 (TSQA) to exit → launches wave-2F. No resume logic anywhere (per the no-resume rule); ~2.5-3h after wave-1 → both done ~6-7h total from 20:02.
- **ADOPTION RULE across both waves:** a knob ships only if it wins (or at minimum doesn't hurt) on BOTH the input side (TSQA acc) and the output side (KS-MSE) — pure-token in/out means ONE recipe serves both.

#### P. WAVE-1 INTERIM — replication guard LANDED (20:33)
- **A0 (new ablate harness, defaults): 0.9360 s0 / 0.9380 s1** vs audit 0.9415/0.9540 → **systematic ≈−1.1pt harness-generation offset** (audit ran pre-refactor code). Seeds agree within 0.2pt → new harness self-consistent; **all wave arms read against A0 mean 0.9370**, not the audit numbers. Per-task baseline: Outliers .992-.994 / Trend .994 / Volatility .983-.989 / **Seasonality .751-.764 = the only headroom task** (aux-loss must move THIS).

#### Q. WAVE-1 INTERIM VERDICTS (as of 21:42; baseline A0 = 0.9360/0.9380, Seasonality .751/.764)
- **A1 aux λ=0.1: 0.9300/0.9480** — seed-split, net-neutral aggregate; Seasonality noisy (.740/.827).
- **A2 aux λ=0.5: 0.9205/0.9340** — aggregate −1pt BUT **Seasonality +2.8pt CONSISTENT (.781/.790)** = mechanism REAL, dose too heavy (taxes Outliers/Volatility). → combo round: λ≈0.25 + annealed-λ (0.5→0).
- **B1 embed-wd0: 0.9255/0.9325 (−0.8) REJECTED** — the blanket 1e-2 decay on the 51.7M embedding group is accidentally load-bearing regularization (at ep3).
- **B2 grad-mask+wd0: 0.9185/0.9115 (−2.2) REJECTED** — full-vocab embedding fine-tune = useful adaptation capacity, NOT damage. Paper sentence: trainable-embeddings-with-decay is the right default for token-injected signals.
- **D2 mamba2-1.3b: 0.9600/0.9310** — mean +0.85 over 370m-A0 but 2.9pt seed spread; needs the same-scale D1 + a within-harness mamba-1.4b reference (queued for combo round).
- Caveat logged: B-verdicts are at ep3; 30-ep official runs accumulate ~10× more decay — acceptable, we ship the tested recipe.

#### R. WAVE-1 VERDICTS, part 2 (22:35)
- **C1 embed-lr 5e-4: 0.9220/0.9545** — seed-split, mean ≈ baseline → no adoption. **C2 embed-lr 1e-3: 0.9015/0.9200 (−2.6) REJECTED** (destabilizes). Numeracy init already places bin rows well — they don't need a hot LR; 1e-4 unified stays.
- **D1 mamba2-370m: 0.8475/0.8445 — REJECTED HARD (−9pt both seeds, weak on ALL tasks).** With D2's 1.3b noise (0.960/0.931), Mamba-2 = NOT a free win. Suspects: AntonV conversion quality and/or peft's in_proj-only LoRA surface on mamba2 (mamba1 gets in/x/dt). Revisit only as full-FT/wider-adapter experiment, NOT in this recipe lock. **mamba-1 stays the workhorse.**
- Seed-0-weaker-than-seed-1 pattern is consistent across arms (train-subset luck) — the paired-seed rule is earning its keep.

#### S. ★★ WAVE-1 COMPLETE (22:42) — WARM-START IS THE HEADLINE
Full table (acc s0/s1; baseline A0 0.9360/0.9380, Seasonality .751/.764):
| arm | s0 | s1 | verdict |
|---|---|---|---|
| A1 aux .1 | .9300 | .9480 | seed-split, neutral |
| A2 aux .5 | .9205 | .9340 | −1pt agg, **Seas +2.8 consistent** → tune λ |
| B1 wd0 | .9255 | .9325 | REJECT |
| B2 mask | .9185 | .9115 | REJECT |
| C1 elr5e-4 | .9220 | .9545 | seed-split, no adopt |
| C2 elr1e-3 | .9015 | .9200 | REJECT |
| D1 m2-370 | .8475 | .8445 | REJECT HARD (−9) |
| D2 m2-1.3b | .9600 | .9310 | noisy, needs 1.4b ref |
| **E1 warm-start** | **.9680** | **.9790** | **★ ADOPT: +3.2/+4.1, Seasonality .892/.916 (+14/+15!)** |
- **WARM-START = the night's discovery.** KernelSynth forecast-pretrain → TSQA finetune wins BOTH seeds by 3-4pt; nearly all of it is Seasonality (periodic GP kernels taught periodicity that instruction-tuning alone never did). Mean 0.9735 vs 0.937. Validates the **signal-native pretraining** paper section AND the recipe. Caveat for later: both seeds share ONE pretrain ckpt (r64, seed0); robustness re-check vs a fresh F0 ckpt from wave-2F.
- Combined story for the paper's recipe section: "we tried the obvious knobs (embedding treatment, LR splits, Mamba-2) — all fail; what matters is WHAT the model saw before instruction-tuning (warm-start) and WHAT it predicts during it (aux loss, dose-sensitive)."
- **COMBO WAVE (G) queued behind wave-2F:** G0 mamba-1.4b within-harness ref ×2 · G1 aux .25 ×2 · G2 warm+aux .25 ×2 · G3 warm+aux-ANNEAL .5→0 ×2 · G4 base ep6 ×2 · G5 warm ep6 ×2 (does the warm gain survive longer training?). ~4h.

#### T. PAPER STRUCTURE DECIDED (user, 23:00) + full overnight chain armed
- **Table 1 = architectural justification:** SP vs Flamingo vs Mamba on the OpenTSLM curriculum (stage1 TSQA → later stages), **current hyperparams, PLAIN recipe (no warm-start/aux)** — isolates architecture/representation. **Main paper method = Mamba + signal-native pretraining (warm-start) [+ aux-λ if combo validates]** on top, as our contribution.
- → official benchmark stays on current hyperparams; ablation winners do NOT get folded into the table runs (they're the method, not the baseline recipe).
- **patchsize1_mamba.patch APPLIED to curriculum_learning.py** (compiles; no-op stage1, correct for stages 2+; working-tree change, commit later).
- **CHAIN (all armed):** wave-2F forecast (running) → wave-3G combo (chainer 73235) → **OFFICIAL stage-1 TSQA 4-leg benchmark** (`chain_official.sh` — quarantines any partial results dirs first = fresh-start guarantee, then run_official_tsqa.sh). ETA: combo done ~05:30 → official ~05:30+14-20h. Stages 2-4 = explicit user go after Table-1 stage-1 lands (also LoRA-confound decision still open for CoT stages).

#### U. WAVE-2F PRUNED (user, 23:08): TSQA-rejected knobs don't get a forecast hearing
- User rule: a knob that clearly hurt wave-1 is dead even if it helps the output side (one recipe, input side is the product). → **dropped FB2 (grad-mask) + FD1 (mamba2-370m) from the queue** (6→ wait, 4 runs cut). FB1 ×2 was already >50% done → letting it finish (free data point). **KEPT: FC1** (TSQA was a seed-split, not a loss — and wave-2F knobs touch the PRETRAIN phase, which may legitimately differ from the finetune recipe: a better pretrain ckpt = better warm-start) + **FS1/FS2 soft-sigma** (output-only knob, no TSQA verdict exists).
- New wave-2F = 10 runs, ends ~00:45; combo ~00:45-04:45; official benchmark starts ~04:45.

#### V. WAVE-2F COMPLETE (01:17) — pretrain recipe CONFIRMED, soft-sigma REJECTED
KS-MSE (s0/s1; lower better; naive 0.631/0.688): **F0 base 0.4719/0.4556** · FB1 wd0 0.4651/0.4460 · FC1 elr5e-4 0.4651/0.4477 (both ≈noise, ~1.5-2% rel) · **FS1 σ=1 0.5342/0.5038 (−11/−13%) · FS2 σ=2 0.5910/0.5453 — REJECTED, clean monotone dose-response in the WRONG direction** (soft targets flatten the dist → hurt greedy point decode; CRPS caveat on record for CiK later).
- **Net wave-2F: the output-side recipe was already right (hard CE, unified lr, wd 1e-2).** Value delivered: fresh r16 F0 ckpts (`checkpoints/ablateF/F0_base_s{0,1}.pt`) for the warm-start robustness check + confirmation no TSQA-side knob damages forecasting.
- G4 base-ep6 ×2 hand-launched early (00:30, idle GPUs); combo wave (chainer) takes over now.

#### W. ops note (01:20): hand-launching ahead of a chained wave = MISTAKE — don't repeat
Hand-launched G4 early on the idle forecast-tail GPUs (00:30) but (a) forgot the wave workers' OMP_NUM_THREADS=12 → 0.96s/it CPU-throttled vs 0.55 normal, (b) the chained combo wave then doubled onto the same GPUs. Killed the pair (no-resume = clean), re-queued G4 as files appended to the LIVE combo queue dir (workers re-scan; this works). **Rule: feed the queue, don't race it.**

#### X. ★★ WAVE-3G COMPLETE (06:10) — METHOD = WARM-START ONLY; the ep6 story is STABILITY
| arm | s0 | s1 | read |
|---|---|---|---|
| G0 m14-ref | .9265 | .9345 | 1.4b < 370m @ep3 (slow convergence); m2-1.3b (.9455 mean) actually ≥ m14 within-harness → scale-up footnote |
| G1 aux.25 solo | .9165 | .9110 | aux solo REJECTED at all 3 doses |
| G2 warm+aux.25 | .9570 | .9535 | aux SUBTRACTS from warm (−1.1/−2.6) |
| G3 warm+aux-anneal | .9650 | .9635 | anneal softens the tax, still ≤ warm alone |
| G4 base-ep6 | **.9785** | **.9355** | **BIMODAL: s0 finds periodicity (Seas .926), s1 memorizes (train-loss→0 by ep3, Seas stuck .745)** |
| G5 warm-ep6 | .9800 | .9840 | consistent; Seas .935/.937 both seeds |
- **FINAL METHOD CLAIM (3 parts):** warm-start (KS forecast-pretrain → IT) = (1) **+3-4pt at short budgets** (sample-efficiency), (2) **eliminates a seed-dependent failure mode** — plain training sometimes converges to a memorization minimum that never learns seasonality (G4-s1), warm-start always finds the periodic solution, (3) **at-convergence level 0.980-0.984 consistent** vs plain's 0.936-0.979 lottery. Aux loss = clean negative ablation (mechanism real solo, subsumed+harmful with warm-start).
- Combo wave DONE → official Table-1 benchmark auto-firing (plain recipe, 4 legs, from scratch). E2 warmF0 robustness pair still finishing on 0/1 (~06:30, brief overlap with SP leg = acceptable).

#### Y. PAPER STORY LOCKED (user, ~07:30) + draft written + 2×2 completed
- **Story arc (user):** (1) architectural superiority of mamba+tokenized vs attention-softprompt / attention-flamingo / **attention-tokenized** on OpenTSLM analysis tasks; (2) beat architectures on signal OUTPUT (transformer+MSE-head / +decoder baselines); (3) the recipe (signal-native pretraining); (4) compete vs strong public checkpoints (Chronos/TimesFM/ChatTS).
- **KEY REFRAME: part 1 = 2×2 FACTORIAL** {attention,SSM}×{encoder,tokens}. The attention+tokens cell separates interface-vs-backbone AND kills the LoRA confound (same tokens+LoRA r16, only backbone differs). **It needs NO new code:** MambaTSLM is backbone-agnostic (AutoModelForCausalLM + else-branch LoRA targets q/k/v/o/gate/up/down; right-pad-no-mask is causal-safe for attention; gen left-pads WITH mask). Leg = `--model MambaTSLM --llm_id meta-llama/Llama-3.2-1B`. **Armed: `chain_llamabins.sh`** (fires after Mamba legs → "2x2 TABLE COMPLETE").
- **Draft: `paper/main.tex`** — full skeleton w/ real numbers (SP 0.767@conv, warm-start table, rejected-ablations table, KS-MSE) + \pend{} (in-flight) + \todo{} (unscheduled: transformer+MSE/decoder forecast baselines, public-ckpt table, efficiency/long-signal exhibits, figures from loss_history files). No pdflatex on box — compile locally.
- **Speed-safe set applied** (curriculum, post-Flamingo launches): TF32 matmuls + cudnn.benchmark + stage1 eval cap 2000→64 tokens + tqdm throttle. Deferred (needs validation): batched eval (bs=1 today!), bf16-Mamba, dataloader workers.
- NEW BASELINES STILL UNSCHEDULED: transformer+MSE-head & transformer+AR-decoder (signal-output comparison), public checkpoints.

#### Z. AUTONOMOUS HANDOFF (user away until ~after mamba_370 leg, ~08:55) — DECISION RULES ON RECORD
- **Chain armed end-to-end:** Flamingo(running) → wave-4 → mamba_14 → mamba_370 → llama_bins ("2x2 TABLE COMPLETE") → post-2×2 (`chain_post2x2.sh`: real-data forecast eval of pretrain ckpts incl. remix arms + bs1-vs-bs8 eval-equivalence for SP/Flamingo). CiK pipeline NOT resurrected (archived-era conda env + 3-step + serial = poor unattended value); replaced with `run_real_eval.py` on local Monash held-out tails (cleaner, answers transfer + remix questions).
- **MY standing decision rules (pre-committed):**
  1. **bins (H5):** rerun token legs at 512/1024 ONLY if it beats 256 on BOTH seeds by ≥0.5pt (vs A0 .936/.938); else keep 256 (lit says expect null at our scale).
  2. **corpus remix (H1-H3):** judge vs E2 reference (.947/.953 — the matched 3000-step-r16 warm-start), TSQA side; tie-break on real-data eval ratio. Winner becomes THE method pretrain corpus (main paper), NOT a Table-1 change.
  3. **TSMixup (H4):** adopt into method pretrain if it doesn't hurt TSQA-warm and helps real-data ratio.
  4. **decode (H6):** if median-of-20 ≤ greedy KS-MSE, adopt sampling for forecast evals (CRPS needs it anyway).
  5. **Llama+bins ep1 sanity:** verify finite losses + sane trajectory; kill+report if NaN (never exercised config).
  6. **No core-file commits until chain empties** (legs import from disk).
- Tooling: `run_real_eval.py` (z-scaled MSE ratio vs naive, per-dataset + agg), `run_eval_equiv.py` (string + answer-letter mismatch rates).

#### AA. FLAMINGO LEG: trained CLEAN, eval CRASHED on a 2nd upstream Flamingo bug — FIXED, eval-only chained
- Flamingo early-stopped correctly (ep11, best val 0.1454@ep8, min_delta math checks out) → final eval crashed: `OpenTSLMFlamingo.generate` passes `eos_token_id`/`pad_token_id` to open_flamingo's FIXED-signature `Flamingo.generate` → TypeError on all ranks. The 87837c9 fix covered forward/init/device; **generation was never smoke-tested** (lesson logged). Fix = filter kwargs to the supported set (OpenTSLMFlamingo.py:265-ish); smoke-verified generate OK.
- **Ruling under no-resume:** training completed per protocol; eval = pure function of saved ckpt → **eval-only is NOT a resume.** Chained `chain_flamingo_eval.sh` (GPU 3, fires at "2x2 TABLE COMPLETE", parallel with post-2×2 on GPUs 0-2). Wave-4 proceeded on schedule meanwhile (launched 09:28).
- NOTE for the table: Flamingo's converged ckpt = ep8 (val 0.1454) per min_delta bookkeeping.

#### AB. ★ PROTOCOL v2 (user catch, verified against the PAPER, ~09:50) — early-stop + SP-LoRA corrected; baselines RERUN
- **User flagged min_delta too aggressive → CONFIRMED against arXiv 2510.02410:** paper = "Up to 200 epochs with early stopping (patience = 5 epochs)", NO min-delta. Our 0.01 ABSOLUTE delta was a per-model-unequal relative bar (7% of Flamingo's 0.14 val vs 2.5% of SP's 0.40) and provably cut Flamingo mid-improvement (abs best 0.1366@ep11 vs delta-counted best 0.1454@ep8).
- **User asked whether SP LoRA gating is in the paper → NO:** paper lists SP learnables as "TimeSeriesEncoder, MLP, and LoRA" with NO stage gating; the stage-3+ gate exists only in the released code. Paper-faithful SP = LoRA from stage 1 (also kills the LoRA confound). Flamingo per paper = encoder+cross-attn, no LoRA (unchanged).
- **EDITS (curriculum_learning.py + model_config.py, compile-checked):** EARLY_STOP_PAT 3→5 · min_delta 0.01→1e-4 · stage1 ceiling 30→200 · stages_with_lora → ALL stages (SP-only path; 2 duplicate function defs both fixed). Queued mamba/llama_bins legs inherit protocol v2 automatically (launch from disk after wave-4).
- **RERUNS armed (`chain_rerun_baselines.sh`):** after post-2×2 + old-protocol Flamingo eval-only (kept as an early-stop-sensitivity datapoint), archive old legs → `results_protocol_v1_archive/` → rerun sp_1b_v2 (LoRA@stage1) + flamingo_1b_v2 from scratch. **Old SP 0.767 and Flamingo's trajectory = protocol-v1, NOT Table-1 numbers.**
- Chain now: wave-4 (running) → mamba_14 → mamba_370 → llama_bins → [post-2×2 ∥ flamingo-eval-v1] → sp_1b_v2 → flamingo_1b_v2. ETA stretches into tomorrow morning; all autonomous.

#### AC. FULL PAPER-PROTOCOL AUDIT (user request, ~10:05) — 2 more violations FIXED, 1 deliberate deviation DOCUMENTED
- **Fixed in addition to §AB:** (1) **SP LoRA LR 1e-4→2e-4** (paper: "encoder 2e-4, LoRA 2e-4, projector 1e-4"; repo wrongly used projector LR — curriculum_learning.py:264); (2) **WARMUP_FRAC 0.03→0.10** (paper: "10% of total training steps"; model_config.py). Both before ANY v2/queued leg launches → all Table-1 legs under one corrected protocol (= protocol v2 final).
- **Verified compliant:** AdamW+wd 0.01; Flamingo enc+xattn 2e-4 no-LoRA; Llama-3.2-1B; normalization+stats-in-text (dataset pipeline). **Unspecified in paper → keep repo:** LoRA r16/α32, bs4 (eff-16).
- **DELIBERATE DEVIATION (documented for the paper):** paper stage-1 = TSQA+M4 JOINT encoder warmup; we train TSQA-only for Table-1 (identical across legs; paper reports no TSQA acc — it's our table design). ★ For stages 2-4 vs published HAR/Sleep numbers: adopt the paper's joint structure (stage2 CoT possibly multi-task joint too — check when scoping).

#### AD. PROTOCOL v2.1 FINAL (user, ~10:15): ceilings 20/stage + min_delta 0.001
- User: 200-ep ceiling overkill → **all 5 stage ceilings = 50** (final; was briefly 20) (compatible: patience-5 does the real stopping work; reviewer-defensible). **min_delta = 0.005** (final; was briefly 0.001). Resume-verification dropped by user ("20 is fine") — noted anyway: ckpts DO save optimizer+scheduler+LoRA states (SP `save_lora_state_to_checkpoint` exists upstream), so extension is mechanically possible; LR-schedule caveat (linear decay tied to num_epochs) means prefer from-scratch if a ceiling is ever raised.
- **Protocol v2.1 = ceiling 50 ×5 stages · patience 5 · min_delta 5e-3 · SP LoRA all-stages @2e-4 · warmup 10% · TF32 · eval-cap 64 (stage1).** All Table-1 legs (mamba_14, mamba_370, llama_bins, sp_1b_v2, flamingo_1b_v2) launch under this.

#### AE. ECG FEASIBILITY PROBE (user request, ~10:30) — FITS at both scales; full-corpus training = multi-day
`tslm_study/probe_ecg_feasibility.py` (synthetic, exact ECG-QA geometry: 12×1000 = 12k signal tokens + ~300 CoT text; run SHARED with a wave-4 job → conservative numbers):
- **370m:** bs1 no-gc **1.64s/it @ 21.5GB** (7k tok/s) · bs2+gc 4.49s/it @ 21.1GB · bs4+gc OOM(shared). **1.4b:** bs1+gc **4.38s/it @ 15.7GB** · bs2 OOM(shared, borderline exclusive). Losses finite — right-pad path holds at 12k tokens.
- **Projections:** full ECG-QA (159k train) ≈ 18h/EPOCH @370m 4×DDP → paper config = subsample (~40k ≈ 4.5h/ep). Eval: 31s/sample bs1 gen at 12k prompt → MUST batch + subsample (≥2000 per eval-rigor). Framing caution: 12k ctx fits a 1B transformer too — the honest claims = full fidelity (encoders compress 12k→hundreds of soft tokens), gentle linear memory (15.7GB @1.4b), headroom toward 100k+ Holter-class where attention actually dies.

#### AF. TF32 INQUIRY CLOSED (user, ~10:50) + speed probes
- **Measured (fp32 curriculum configs, same-GPU A/B):** SP 0.247→0.237s/it (~4%: its Llama backbone is bf16 @ OpenTSLMSP.py:46 — TF32 path untouched); **MambaTSLM fp32 0.918→0.507s/it = 1.81×** (genuinely full-fp32 in curriculum). TF32 helped NOTHING that has already run (waves = bf16; SP/Flamingo-v1 predate the flag); the two Mamba Table-1 legs get the 1.81×.
- **grad-ckpt A/B (same contention):** ON 1.216 vs OFF 0.926s/it = 1.31×; worst-case TSQA bs8 all-512 w/o gc = 9.9GB → flip harness default OFF after wave-4 (queued).
- **ECG scaling curve (370m bs1):** 1.2k tok 316ms / 6k 917ms / 12k 1628ms ≈ linear 0.12ms/token marginal, throughput RISES 3.8k→7.4k tok/s (fixed-overhead amortization) — Figure-ready for §4.7.
- **Wave-4 interim:** ★ **H1 remix-90/10 ADOPTED-pending-output-side: TSQA-warm 0.9730/0.9715 vs E2 0.947/0.953 (+2.6/+1.9 BOTH seeds)**; H2 50/50 mixed (+1.6/−0.1); dose-response 90/10 > 50/50; H1 ≈ E1-original with HALF the pretrain steps at r16 — corpus was the missing ingredient. KS-MSE on remix ckpts worse in-distribution (expected); real-data eval post-2×2 is the output-side judge. Pending: H3 real-100, H4 TSMixup, H5 bins, H6 decode.

#### AG. PLAN REVISED (user, ~11:10): epoch-budget DIAGNOSTIC before Table-1 legs
- **User steer (saved to memory `feedback-informed-protocol-tuning`):** choosing round shared epoch budgets (20/30/50/100) INFORMED by where our model fares well = standard lab practice, not p-hacking; blind choices just gamble in a random direction. My over-strict pushback recalibrated; line remains only at outcome-reverse-engineered precise values.
- **NEW SEQUENCE:** wave-4 (running) → **mamba_14 EPOCH DIAGNOSTIC** (`chain_diagnostic.sh` armed: TSLM_PATIENCE=999, TSLM_MAX_EPOCHS=100, TSLM_SAVE_EVERY_EPOCH=1 → per-epoch trainable ckpts; ~15-20h) → post-hoc acc-vs-epoch sweep (test subset, batched) → **pick ROUND shared epoch budget (≥20 floor, user)** using Mamba's curve + SP/Flamingo val histories (all-models-converged check) → run ALL Table-1 legs under final protocol. Auto-leg chainer (chain_ab_then_mamba) KILLED — downstream legs now manually sequenced; llamabins/post2x2/rerun chainers dormant (their trigger strings won't appear until I write them).
- **Env overrides added to curriculum (additive, default-off, compiled):** TSLM_PATIENCE / TSLM_MAX_EPOCHS / TSLM_SAVE_EVERY_EPOCH. ⚠ Diagnostic writes results/mamba_1_4b_hf/... → QUARANTINE before the real table leg (metrics.json would trigger auto-skip + no-resume rule).

#### AH. ★★ WAVE-4 COMPLETE (13:35) — corpus remix ADOPTED, decode trick ADOPTED, bins/TSMixup REJECTED
Verdicts (TSQA-warm vs E2 ref .947/.953; plain baseline .936/.938):
| arm | s0/s1 | verdict |
|---|---|---|
| **H1 remix 90% real/10% KS** | **.9730/.9715** | **★ ADOPT (+2.6/+1.9 both seeds, tightest variance)** |
| H2 50/50 | .9625/.9515 | reject (mixed) |
| H3 100% real | .9430/.9720 | reject (2.9pt spread — synthetic fraction = STABILITY) |
| H4 TSMixup k=3 on KS | .9325/.9405 | reject (lit caveat confirmed: mixup is for heterogeneous REAL corpora) |
| H5 bins 512 / 1024 | .9345/.9520 · .9100/.8995 | no-adopt / reject hard → **256 stays** |
| **H6 decode median-of-20 @T0.5** | KS-MSE **0.3624** vs greedy 0.4826 | **★ ADOPT (−25% MSE, inference-only)**; T0.5<T0.7(0.376)<T1.0(0.405) |
- **METHOD RECIPE LOCKED:** quant/zscore/u256/numeracy + signal-native pretrain on **90/10 real(freq-balanced)/KernelSynth** + IT (LoRA r16, lr1e-4) + **median-of-20@T0.5** decode. Corpus dose-response: 90/10 > 50/50 > both purity extremes — Chronos's ~10%-synthetic optimum replicates on a discriminative task. Pending: real-data output-side eval of H1 ckpts (post-diagnostic).
- grad-ckpt flipped to opt-in (--grad-ckpt) in both harnesses (measured 1.31× tax, 9.9GB worst-case without).
- **13:35 diagnostic launched.** 17:14 status: ALIVE at ep8 (~26min/ep), val 0.064→0.028 still improving, 8 per-epoch ckpts, timing columns working. **Windows-update PC reboot killed only session monitors (re-armed); ZERO GPU loss** (all jobs nohup'd on the box).

#### AI. DIAGNOSTIC CALLED (user, ~21:05) → EPOCH BOUND = 30 → TABLE-1 FINAL SEQUENCE LAUNCHED
- **Diagnostic verdict (17 epochs run):** best-val ep12 (0.0124) then ep17 (0.0066) — under a 200-ep-stretched LR schedule whose WARMUP ALONE = 20 epochs (LR still rising the whole run; ep16 spike 0.0938 = near-peak-LR instability, instantly recovered). Discounted to a real schedule → Mamba's need ≈ high-teens. **User set ceiling = 30** (round, ≥20 floor honored, all models converge inside: SP ~21-24, Flamingo ~11-17, Mamba ~12-17 stretched).
- **PROTOCOL v3 FINAL = ceiling 30 ×5 stages · patience 5 · min_delta 0.005 · SP LoRA ungated @2e-4 · warmup 10% · TF32 · eval-cap 64 · Elapsed_s/World_Size timing.**
- **`chain_table1_final.sh` RUNNING (21:08):** step-0 eval-only of diagnostic best ckpt (for the record + acc-vs-epoch anchor) → quarantine diagnostic + v1 Llama_3_2_1B dirs (auto-skip/resume hazards) → 5 legs from scratch: mamba_14_v3 → mamba_370_v3 → llama_bins_v3 → sp_1b_v3 → flamingo_1b_v3. Dormant superseded chainers (llamabins/post2x2/rerun/flamingo-eval) all killed. Post-table extras still owed: real-data eval of H1 remix ckpts, bs1-vs-bs8 equivalence, flamingo-v1 eval-only footnote, acc-vs-epoch sweep over diagnostic ckpts (archived, 17 ckpts).

#### AJ. ★ USER CAUGHT A REAL BUG (06:00): min_delta gated CHECKPOINT SELECTION, not just patience → DECOUPLED → v3b relaunch
- mamba_14_v3 evidence: ep19 val 0.0038 strictly < ep14's 0.0069 but improvement < δ0.005 → NOT SAVED; eval used ep14 (acc 0.9933 vs diagnostic-ep17's 0.9950). Coupled semantics = indefensible (Keras/Lightning: δ → patience only, best ckpt = raw best).
- **FIX (curriculum_learning.py): `counts` (δ-gated, patience) decoupled from `should_save` (raw-best, ckpt selection); `best_raw_val_loss` tracker added; DDP all-reduce carries both flags. Rehearsed end-to-end (130m, capped): sub-δ improvements now saved, worse epochs not, stop math unchanged ✓.**
- **PROTOCOL v3b = v3 + raw-best selection (stopping identical).** mamba_14_v3 (0.9933) + partial 370m archived → `results_protocol_v3coupled_archive/`. **`chain_table1_v3b.sh` RUNNING (06:11):** 370m → llama_bins → sp → flamingo → mamba_14 (rerun last). ETA ~late tonight.
- NOTE the asymmetry this fixes: δ-floor-effect penalized SMALL-val models (Mamba) at selection time — the fix is uniform but mostly helps us; documented honestly (stop rule unchanged, selection now standard).

#### AK. PROTOCOL v4 = FINAL (user, 06:15): min_delta back to upstream 1e-4 — "the defaults might be the best"
- δ 0.005 → **1e-4 (upstream original)**, keeping the raw-best decoupling (that was a bug fix, not a hyperparam). **v4 = ceiling 30 · patience 5 · δ1e-4(patience-only) · raw-best ckpt · SP LoRA ungated @2e-4 · warmup 10% · TF32 · eval-cap 64 · timing columns.** Net of two days of protocol churn: every deviation from upstream/paper defaults that we TESTED lost or washed; final protocol ≈ paper protocol + bug fixes + ceiling 30 (diagnostic-justified round number) — now with an audit trail instead of faith. Effect of δ1e-4: sub-0.005 improvements reset patience → legs train a bit longer (Mamba ~ep19+→, Flamingo continues past 11) — truer convergence, ~+4-8h table ETA.
- 30-min v3b 370m partial archived. **`chain_table1_v4.sh` RUNNING (06:17):** mamba_370_v4 → llama_bins_v4 → sp_1b_v4 → flamingo_1b_v4 → mamba_14_v4. ETA tomorrow morning. Reference points banked: diagnostic 0.9950 (stretched LR), v3-coupled 1.4b 0.9933@ep14.

#### AL. TABLE-1 v4 LANDINGS (overnight 06-11→12) — TSQA SATURATES under the correct protocol
- **mamba_14_v4 = 0.9983** (8/4800 errors; best ep28/30, val 0.0015; 13.0h wall = 52 GPU-h).
- **sp_1b_v4 = 0.9946** (26/4800 errors; best ep29/30, val 0.0053; **5.9h wall** = 23.7 GPU-h) — paper-faithful LoRA transforms SP (v1 frozen: 0.767). The v1 "0.3-0.4 val cap" was the frozen backbone, NOT the encoder bottleneck.
- **NARRATIVE IMPLICATION (honest):** stage-1 TSQA at convergence is NEAR-SATURATED for both architectures — headline framing shifts from "Mamba wins TSQA big" to (a) **3.2× error reduction on the saturated task** (0.17% vs 0.54% err), (b) **the capability axis** (signal OUT: SP structurally can't), (c) **long-signal regimes** (ECG probe), (d) the 2×2 interface-vs-backbone decomposition (llama_bins pending), (e) warm-start method results (0.98 at 1/5 the epochs etc.). NOTE: SP converged 2.2× faster in wall-clock at this scale — efficiency claims must be regime-specific (long signals), not blanket.
- Flamingo_1b_v4 training since 01:26.

#### AM. ★ FLAMINGO CHECKPOINT DEFECT (06-12 morning) — official 0.4667 = ARTIFACT, investigation logged
- **flamingo_1b_v4 "0.4667" is NOT a result:** generation collapsed to majority-c (83% of preds) while recorded val = 0.0688. Forensics: the saved best ckpt scores **1.31 val on reload** (19× worse than recorded); v1-era flamingo ckpt same defect (0.5695 vs 0.1454, 4×) → **upstream bug, predates all our edits, grows with training scale**.
- **Eliminated:** key mismatch (exact match, missing=0/unexpected=0) · all-zero gates (gates grow 2ep .0014 → 8ep .028 → 30ep .043, consistent) · weight-tying desync (tied in file AND fresh model) · duplicate model./llm. key-family divergence (568 pairs identical) · BatchNorm/eval-mode (no BN; val correctly eval()) · torn writes (loads clean) · save/load mechanics per se (**2-rank/2-ep and 8-ep capped repros round-trip fine**, ≤6% gap).
- **Constraint that narrows it:** SP (encoder/projector/LoRA-state branch) round-trips fine (0.9946 official eval IS a reload!) and Mamba (trainable-only branch) was bit-exact-verified → defect is specific to the **Flamingo full-state_dict else-branch at full scale**.
- **INSTRUMENTATION ADDED (env-gated TSLM_VERIFY_SAVE=1, compiled):** (a) fingerprint_loss stamped into every ckpt (live model, fixed val batch, at save time); (b) post-save file-vs-memory tensor-equality audit. **`chain_flamdiag.sh` armed** (fires after EXTRAS DONE): full-data 4-rank 8-ep instrumented Flamingo → locates first divergent epoch + separates save-corruption vs reload/construction mismatch. Official v4 flamingo dir gets archived as evidence (`results_flamingo_v4_archive/`).
- **TABLE-1 status: Flamingo row = pending (ckpt-defect investigation)**; 0.4667 excluded from all claims. Mamba-1.4b 0.9983 + SP 0.9946 unaffected.

#### AN. ★★ FLAMINGO ROOT CAUSE FOUND + FIXED (06-12 evening) — SimpleNamespace hid the TS encoder
- **Census of the v4 ckpt: ZERO encoder tensors saved.** The TS encoder is passed as `SimpleNamespace(visual=encoder)` → not an nn.Module → invisible to `.parameters()` AND `.state_dict()`. Verified live: encoder params (132k) NOT in model.parameters() despite requires_grad=True. Therefore: **(a) the encoder NEVER TRAINED (random init all run — violates the paper's own trainables list, in upstream's code, both v1 and v4); (b) never saved; (c) every reload = NEW random encoder under perceiver/gates tuned to the old one → collapse scaling with training** (v1 4×, v4 19×; mini-repros passed because gates ~0.001 = signal path off). Upstream cause: open_flamingo's contract assumes a reconstructible pretrained CLIP; OpenTSLM swapped in a random-init encoder without updating save/optimizer assumptions. SP immune (registered encoder).
- **FIX: `model.ts_encoder = time_series_encoder`** (OpenTSLMFlamingo.py, registers as real submodule; forward unchanged — vision_encoder.visual references the same object). Structural halves verified live (keys in state_dict; params in parameters(), trainable). Training+round-trip validation = **GATE** inside `chain_flamingo_v5.sh` (armed; fires after EXTRAS DONE): mini-train must move encoder AND round-trip with a different-seed fresh model within 2%, else leg aborts. On pass → **flamingo_1b_v5** (protocol v4 + fix). Instrumented diag killed as redundant (root cause found statically); llama_bins back at full speed.
- **EQUIVALENCE CHECKS (pulled forward, DONE): bs1-vs-bs8 = NO-GO for SP** — 50% answer-letter mismatch; bs8 outputs are garbled (padding/position bug in SP's batched generate). **Stages 2+ keep bs1 eval** (64-token cap remains their only speedup); Mamba's bs8 path already validated by the waves. Saved us from silently corrupting stage-2 evals.
- Pending tonight: llama_bins (~04:00) → extras (real-data corpus eval) → flamingo gate+v5 (~midday tomorrow). Table-1 = 4 rows solid + Flamingo v5 pending.

#### AO. ★★ TABLE-1 2×2 COMPLETE (06-13 03:00) — synergy story, not a blowout
Full test n=4800, protocol v4 (ceil30/pat5/δ1e-4/raw-best):
| backbone | interface | acc | errors | val-floor |
|---|---|---|---|---|
| **SSM mamba-370m** | tokens | **0.9992** | 4 | ~0.0012 |
| SSM mamba-1.4b | tokens | 0.9983 | 8 | ~0.0015 |
| Attn Llama-1B | encoder/soft-prompt | 0.9946 | 26 | ~0.005 |
| Attn Llama-1B | **tokens** | 0.9881 | 57 | ~0.019 |
| Attn Llama-1B | encoder/Flamingo | DEFECT→v5 | — | (0.069 recorded) |
- **Factorial read:** (1) tokens fixed → SSM beats attention (+1pt, lower floor 10×); (2) attention fixed → encoder (0.9946) BEATS tokens (0.9881); → **interface & backbone INTERACT: tokens want an SSM, encoders want a transformer.** Our cell tops the table AND is the only one that generates. Cleaner than "we win": the SSM is what *unlocks* the pure-token interface. (3) val-floor spread (0.0012 vs 0.019, ~16×) ≫ acc spread (1.1pt) → TSQA near-saturated, real separation lives in generation + long-signal regimes.
- 370m (cheapest) = most accurate (6.5× fewer errors than SP at ⅓ params, least GPU-h) — efficiency headline.
- paper Table-1 + val-loss fig (5 curves) updated. **TSLM_VERIFY_SAVE removed? NO — keep.** Protocol now v4.1 (transfer stages ceil 10, stage-1 ceil 30; user 06-12).
- ★ **BOX IDLE — user migrating to 16×A100.** All chainers disarmed (extras/flamingo-v5/etc). On return: smoke-test DDP+kernels on new box, re-arm in 4-parallel-quartet form, run extras → flamingo gate+v5 → stages 2-4.

#### AP. ★ BATCHED EVAL FIXED (06-13) — SP was 3 stacked bugs, not "unfixable"; now bs8≈bs1
User rejected the "encoder models can't batch, use bs1" workaround. Real root cause = SP's signal path was NOT batch-invariant via THREE bugs:
1. **Encoder attends over padding** (`TransformerCNNEncoder` ran the TransformerEncoder with no `src_key_padding_mask`) → a short series' patches contaminated by batch-max padding. FIX: added `patch_padding_mask` arg, plumbed from `pad_and_apply_batch` (ceil(true_len/patch) real patches). Soft-prompt Δ(alone-vs-batched) **2.05 → 0.0078** (bf16 noise).
2. **Padding patches injected as fake signal tokens** — reassembly appended the FULL `ts_proj[i]` (all N padded patches) into the LLM stream w/ all-ones mask. FIX: keep only `[:n_real_patches]`.
3. **Right-padded generation** — decoder-only must left-pad; HF also doesn't derive position_ids on the inputs_embeds path. FIX: left-pad + explicit position_ids in `generate`.
- **SAFETY: all 3 are exact NO-OPS at bs1 for TSQA** (series lengths are multiples of patch_size → zero padding) → official SP 0.9946 provably untouched. **VERIFIED** on strong v4 ckpt: bs1-vs-bs8 **64/64 exact**, acc bs1=bs8=0.969 (64-subset); equiv harness 128-sample = 0.8% (single bf16 argmax flip, ~0.04pt impact). loss path batch-consistent. End-to-end curriculum eval@bs8 exit 0.
- **WIRED:** curriculum eval batch size = 8 for MambaTSLM + OpenTSLMSP (both batch-invariant-verified), 1 for Flamingo (encoder not yet mask-plumbed through its perceiver path — follow-up before its stage-2 run if its bs1 CoT eval is the long pole). Override `TSLM_EVAL_BS`. → ~8× faster stage-2+ evals for SP+Mamba.
- Ops note: the original "50% mismatch NO-GO" reading was inflated by `run_eval_equiv` flat-loading SP wrong (SP saves encoder_state/projector_state/LoRA separately, not flat) — harness FIXED to use `enable_lora()+load_from_file`. The encoder Δ=2.0 bug was real & independently measured. Also caught: weak v1 ckpt's near-tied logits flip under bf16 batching (model-conditioning, not a bug) — always validate batch-invariance on a STRONG ckpt.

#### AQ. EPOCH-BOUNDARY RESUME built (TSLM_RESUME=1) — relaxes the no-resume rule for long stages, PENDING proof
- User wants a continue-from-checkpoint that doesn't LOSE shuffle order / LR / Adam state (not bit-identical). Built as a **pure addition** (default-off; best-model save/load paths untouched → zero regression to the table numbers).
- **Saved every epoch (gated):** `latest.pt` (rank0: model+optimizer+scheduler+epoch+best_val_loss+best_raw_val_loss+epochs_no_improve+world_size; atomic os.replace) + `rng_rank{r}.pt` (each rank: python/torch/cuda/numpy RNG). Shuffle already deterministic via existing `set_epoch(epoch)`. RNG restored AFTER model-build draws, right before the loop. world_size-mismatch → refuse resume.
- **PROOF (`tslm_study/test_resume_equiv.sh` + `cmp_resume.py`, RUNNING):** mamba-130m (lora_dropout 0.05 so RNG matters), 2-rank DDP, 8 epochs. A straight vs A2 same-seed straight (= float-noise FLOOR from mamba fused-kernel atomics) vs B 4→resume→8. PASS iff resumed-epoch |B−A| ≤ ~|A2−A| (+ final-weight rel-L2). If PASS → safe to enable for stage-2+ legs so a box-kill on day 3 resumes instead of restarting. Still default-off; no-resume rule stays for anything not using TSLM_RESUME.
- **★ PROVED PASS (04:30):** decisive metric = final-weights rel-L2: **A2-vs-A (noise floor) 1.034e-02 ≈ B-vs-A (resume) 1.039e-02** → resume adds ZERO divergence beyond the mamba-kernel/DDP float-noise floor; resumed model is as close to the reference as a same-seed uninterrupted rerun. (Per-epoch val curve noisier ~2× floor = chaotic amplification at n=1; weight-distance is the stable metric. At epoch5, first post-resume, |B−A| 1.69e-2 < |A2−A| 2.08e-2.) **TSLM_RESUME=1 is trustworthy** — adopt for the multi-day stage-2+ legs (kills resume, not restart). Default stays off; classic no-resume rule governs everything not opting in. Tools: `tslm_study/test_resume_equiv.sh` + `cmp_resume.py`.

#### AR. REAL-DATA forecast eval (06-13, greedy) — synthetic-pretrain ZERO-SHOTS real data ~2× better than naive
- Per-dataset MSE-ratio vs naive (z-scaled, horizon 32, greedy; SOLAR EXCLUDED — flat-zero at night → history std≈0 → z-blowup zMSE 2.4e10 swamps aggregate, run_real_eval bug to fix): **electricity ~0.41-0.57, traffic ~0.40-0.49, nn5 ~0.49-0.69** across {pretrain_ks_370, F0 synth-only, H1-remix s0/s1}. All ≈0.4-0.5 = **models trained ONLY on KernelSynth GPs forecast real electricity/traffic/nn5 at ~half naive MSE, zero-shot.**
- **Corpus verdict: variants COMPARABLE on real-data forecasting** (mixed across datasets/seeds, within noise) → 90/10-remix adoption rests on its clean TSQA win (+2.6/+1.9 both seeds), NOT a forecasting edge. Honest: corpus helps understanding, doesn't differentiate real-data zero-shot forecast.
- median-of-20 cut short (would've blocked HAR ~50min; uninformative for verdict + solar-broken aggregate). TODO: re-run fixed real_eval (solar div-by-0 guard + median20) as a quick side job.
- **MASTER CHAIN running (05:32):** real-eval(skipped, done) → Flamingo-v5 gate+stage1 → HAR×5. GPU-efficiency note: parallelized the real-eval across 4 GPUs after user flagged single-GPU stall (good catch — would've wasted ~25 GPU-min in front of a multi-day chain).

#### AS. ★ FLAMINGO PARAM AUDIT (user, 06-13) — was training 263M LM embeddings (paper says no, SP doesn't) → FROZEN
- User: "double-check we freeze/train the correct Flamingo parts." Empirical trainable/frozen census (CPU build): LLM-core 973M FROZEN ✓ · gated_cross_attn 572M TRAIN ✓ · ts_encoder 0.13M TRAIN ✓ (the §AN fix) · perceiver 2.4M TRAIN (cross-attn machinery) · **LM input embeddings 263M TRAIN ⚠️** (freeze_lm_embeddings default False) · no LoRA ✓.
- **Paper (2510.02410) Flamingo learnables = {TimeSeriesEncoder, cross-attention}** — embeddings NOT listed. **SP census: embeddings FROZEN** (trains LoRA 11.3M + encoder 2.1M + projector 0.26M = 13.6M, matches paper exactly). So Flamingo training 263M embeddings was BOTH non-paper-faithful AND inconsistent with SP (user caught this).
- **FIX: freeze_lm_embeddings=True** in curriculum Flamingo construction → trainable 837.8M→575.2M, embeddings 0. **Verified safe:** frozen-emb trains ≥ as well as trained-emb on identical toy run (0.21 vs 0.27 over 4 capped epochs; new media tokens at frozen init fine, cross-attn does conditioning). Consistent with the SP-LoRA correction (match paper, not released-code default).
- v5 (embeddings-unfrozen) killed at epoch1, partial cleared. **Master chain relaunched 06:44** with frozen-emb Flamingo: gate → flamingo_1b_v5 stage1 → HAR×5. Flamingo trainable now 575M (still the heaviest leg).

#### AT. ★★ TABLE-1 COMPLETE (06-13 14:26) — Flamingo v5 = 0.8904 (valid!), HAR×5 started
- **flamingo_1b_v5 stage1 = 0.8904** (val 0.063@ep30, balanced preds a/b/c ≈ 1589/1562/1649 — NOT v4's collapsed 83%-c artifact). The encoder-save fix + frozen-embeddings gives a VALID reloadable Flamingo. v4's 0.4667 was pure reload-bug garbage. As predicted, v5 val≈v4 (~0.063) but ACCURACY now trustworthy.
- **FULL TABLE-1 (full 4800 test, protocol v4, paper-faithful):** Mamba-370m **0.9992** > Mamba-1.4b 0.9983 > SP 0.9946 > Llama+bins 0.9881 > Flamingo 0.8904. **Flamingo<SP matches the PUBLISHED OpenTSLM ordering** = independent validation our baselines reproduce the paper. 2×2 read holds: tokens want SSM, encoders want attention, SSM+tokens tops it + only token-interface generates.
- paper Table-1 fully filled. **HAR mamba_14 STARTED 14:26** (stage3 from TSQA ckpt, 10ep). Then mamba_370 → llama_bins → sp → flamingo.

#### AU. CoT epoch decision (user, 06-13): KEEP 10 epochs, all 5 legs (revisit if F1 low)
- HAR mamba_14 still descending @ep4 (val 0.718→0.623→0.585→0.562) → 10ep mildly undertrains, BUT user chose to keep 10/all-5 for speed (~2 days vs 5.6 days @30ep×5). HAR = ~53 min/epoch (CoT seqs long, 2× TSQA).
- **Subsample-to-afford-30 idea REJECTED (my input): trades data diversity for repetition (30ep on ⅓ data = same steps, fewer distinct examples seen more times) → likely neutral-or-worse than 10ep-full.** If we ever bump epochs, do it on FULL data + trim legs or use a real early-stop δ.
- **REVISIT TRIGGER (committed): if HAR macro-F1 ≪ published 65.4 → undertraining signal → bump CoT to 30ep (full data) rerun.** Will flag explicitly when first HAR F1 lands, not pass silently. Rationale for 10 being OK meanwhile: all legs equally undertrained → architecture RANKING stays fair even if absolute F1 is a touch low.

#### AV. UNIFORM SAMPLE CAP adopted (user, 06-13): ≤50k train / ≤10k test per stage
- Decision rationale (apples-to-apples defensibility): limit-samples-uniformly > ad-hoc subsample; chose 50k/10k so it stays ABOVE TSQA (38.4k/4.8k) → **Table-1 unaffected, no rerun**. Caps HAR train 68.5k→50k (27%), ECG 159k→50k train + 41k→10k test (the real enabler — ECG only feasible capped). Sleep/TSQA untouched. 10k test = ±~1% margin, ample. Pairs with train-to-convergence + published val curves = defended on BOTH "undertrained" and "data-starved" axes. Caveat: capped train ≠ published-baseline full-data, so our HAR/ECG numbers are for the INTERNAL controlled comparison, not direct vs published 65.4 (add a full-data headline row if beat-published matters per stage).
- **WIRED:** curriculum `_merge_data_loaders` keyed on shuffle (train→TSLM_CAP_TRAIN, eval→TSLM_CAP_TEST); SEEDED-random subset (fixed seed 20260613 → identical across models AND DDP ranks; avoids first-N bias; fixed across epochs). Verified: HAR-train→50k, ECG-test→10k, TSQA/Sleep unchanged, deterministic. `chain_master_har.sh` exports the caps.
- **CONSEQUENCE:** current mamba_14 HAR is FULL-data (no cap, started pre-decision) → on resume, ALL 5 HAR legs RERUN at 50k for consistency. The full-data run is NOT wasted: gives early F1 read (convergence/65.4 check) + a full-vs-50k reference point. Chain still paused-after-mamba_14 per user (GPU reservation).

#### AW. CAP REVERTED (user, 06-13): full data after all; ECG deferred
- "Screw it undo the cap we can worry about ecg later." → removed the export from chain_master_har.sh. HAR/Sleep run FULL DATA. Cap mechanism (TSLM_CAP_TRAIN/TEST, seeded subset) stays in curriculum_learning.py, default-off, dormant for the eventual ECG tractability fix.
- **Upshot: current mamba_14 full-data HAR is now the OFFICIAL leg (no consistency rerun)** — the ~9h is the real run, not a throwaway. Chain still paused-after-mamba_14 (GPU reservation). On resume: mamba_370 → llama_bins → sp → flamingo HAR, all full data, 10 epochs.

#### AX. ECG DROPPED (user, 06-13): "we don't even have to do ECG lol"
- ECG-QA training not required. Curriculum scope now = TSQA + HAR + Sleep (clean, tractable, full data). stage5_ecg_cot commented out of CURRICULUM_STAGES (reversible, like M4). active = [stage1_mcq, stage3_cot, stage4_sleep_cot].
- **Long-signal thesis STILL MADE without an ECG leg:** (a) feasibility probe = SSM trains a full 12-lead 12,248-token ECG @1.6s/step, 21GB, LINEAR scaling (journal §AE); (b) paper's own "Tokenized Finetuned" attention baseline OOMs on ECG (80k tok, >100GB/sample) — corroborates attention-breaks-on-long-signal. So capability gap = memory/throughput figure + paper's data, no multi-day training. Forgo only: demonstrated ECG-QA ACCURACY (secondary to the capability claim).
- **Bonus: the entire OOM-policy / grad-accum / 50k-cap thread was ECG-tractability — now MOOT.** Full data everywhere, no caps, no OOM policy needed. (Paper's OOM convention for reference: per-sample/bs1 bar, ">100GB per sample"; cap mechanism stays dormant in code.)
- Paper §4.6/efficiency: cite the probe (Fig: linear mem/throughput vs length) + paper's Tokenized-OOM row for the long-signal argument; no ECG results row.

#### AY. HAR RUN STOPPED (user, 06-13): don't waste 10ep if we'll want 30
- User suspects we'll want 30ep → killed mamba_14 HAR mid-run rather than finish a 10ep we'd toss. Box IDLE (0 GPU). Partial quarantined → `results_partial_har_killed/mamba_14_stage3_*` (no-resume). Table-1 stage1 intact. Watcher stopped.
- **PENDING (2 user decisions before relaunch):** (1) GPU reservation; (2) epochs 10 vs 30 for CoT stages. Relaunch = set stage3(HAR)+stage4(Sleep) ceiling to chosen N in curriculum, fire 5 HAR legs full-data (parallelize if bigger GPU pool). HAR ≈53min/epoch → 30ep ≈27h/leg.
- Nothing queued/armed. Clean stop.

#### AZ. BATCH-SIZE TEST (user, 06-13) — eff-8 converges marginally faster but NOT WORTH IT; keep eff-16
- Paired test 370m, lr 2e-4 (official), 3ep, seeds {0,1}: **bs16(eff-16) .9290/.9615 mean .9453 · bs8(eff-8) .9350/.9660 mean .9505 (+0.52pt both seeds).** Wall-clock IDENTICAL (1343 vs 1358s — 2× steps each half-compute = same/epoch). No instability at eff-8 (the LR was the fragile knob, not batch).
- **VERDICT: keep eff-16.** The +0.5pt is a convergence-RATE effect at a short budget; at the 50-ep ceiling both SATURATE (~.999) so it vanishes; no wall-time win (same/epoch, both ride to 50 under δ1e-4); and halving breaks eff-16 consistency w/ paper+Table-1+all ablations. Would only matter w/ short budgets or early-stopping — not our setup.
- TSQA-50 relaunched at standard eff-16 (22:42): mamba_14 → mamba_370. (Earlier LR-double request also declined: 2e-4 is paper LR for SP/Flamingo + measured-stable ceiling for Mamba; 4e-4 near the 5e-4 break point, saturated TSQA = nothing to gain.)

#### BA. STATE / NEXT
- **Branch `mamba-integration`, 6 commits on upstream/main:** 72cfed7 model · a47d0c5 curriculum · d761922 experiments · 1d98b61 config · 87837c9 Flamingo-fix. NOT pushed (awaiting user go). + UNCOMMITTED protocol-v2.1/speed/Flamingo-generate-fix/patchsize edits in working tree (fold after chain empties).
- **NEXT:** (1) rerun official TSQA (4xDDP eff-16, 30-ep+min_delta); (2) decide LoRA confound (match-capacity vs each-at-protocol); (3) be SURGICAL on later stages (run only paper-figure stages); (4) push branch + open PRs.
- **Safe:** ahri-signal-output (stash) + main_results/ + results_preview_20260609/ + tslm_study/ (untracked) all persist.
- **OPS LESSON (recurring):** background runs on this box keep getting **killed by an external event** (audit twice, both benchmark attempts) — checkpoint everything to disk, treat watchers as disposable, expect to relaunch.

## 4. Archive
Pre-SSM history (do not resurrect): `research_journal/archive/JOURNAL_2026-06-04_pre-SSM-era.md` — AHRI corpus OFAT study, grounding-gap diagnostic, context-forecasting on CiK, neuro-symbolic Chronos residual-correction (peaked 0.2473, user rejected: "feature engineering + used the opponent as encoder").
